private/

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# out/ is deliberately NOT ignored. The generated report is committed so the
# submission can be read without running anything; `ci run` overwrites it.
.env
